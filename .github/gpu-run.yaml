# Request a GPU run: change this file, commit, push. The run-gpu workflow triggers on every push that changes
# this file and runs `command` at the repository root on a RunPod worker. To re-request an identical command,
# there needs to be a new `request` label. A manual workflow_dispatch ignores this file.
request: mixing_olmo3_7b_seed20260907 # note that a new label re-requests the same command
command: uv run directions mixing --config configs/mixing.yaml --run-id mixing_olmo3_7b_seed20260907 --runs kth_word=/runpod-volume/results/run-35772055428/kth3_learned_olmo3_7b_seed20260907:/runpod-volume/results/run-35771763692/kth3_olmo3_7b_seed20260907 lexical=/runpod-volume/results/run-35538374069/learned_olmo3_7b_seed20260907:/runpod-volume/results/run-35529394971/pilot6_olmo3_7b_seed20260907
gpu_tier: ADA_24         # memory class ("tier")
gpu_type: ""              # exact device name to pin instead of the tier, e.g. "NVIDIA RTX A4500"
datacenter: EUR-NO-1       # data center of the `directions` network volume
flash_env: ci          # Flash environment; one run at a time per environment
timeout_minutes: 340
max_output_mb: 8
